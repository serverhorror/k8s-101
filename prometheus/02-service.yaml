apiVersion: v1
kind: Service
metadata:
  name: prometheus
spec:
  type: NodePort
  ports:
  - name: prometheus-http
    port: 80
    targetPort: 9090
    nodePort: 30280
    protocol: TCP
  selector:
    app: prometheus